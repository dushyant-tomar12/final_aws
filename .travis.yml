sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t dushyant-tomar12/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true dushyant-tomar12/react-test npm test

after_success:
  - docker build -t dushyant-tomar12/multi-client ./client
  - docker build -t dushyant-tomar12/multi-nginx ./nginx
  - docker build -t dushyant-tomar12/multi-server ./server
  - docker build -t dushyant-tomar12/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dushyant-tomar12/multi-client
  - docker push dushyant-tomar12/multi-nginx
  - docker push dushyant-tomar12/multi-server
  - docker push dushyant-tomar12/multi-worker
